<% if !current_user %>
  <div class="d-flex justify-content-center text-center">
    <h4>Для того, чтобы присоединиться к сообществу, войдите с помощью своего аккаунта вконтакте</h4>
  </div>
  <div class="d-flex justify-content-center"><%= link_to "<button>Войти</button>".html_safe, user_vkontakte_omniauth_authorize_path %></div>
<% else %>
  <div class="d-flex justify-content-center text-center">
    <h4>Добро пожаловать, <%= current_user&.name %>!</h4>
  </div>
<% end %>